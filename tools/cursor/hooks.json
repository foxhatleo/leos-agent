{
  "$doc": "Cursor CLI hook registration fragment, ownership-merged into ~/.cursor/hooks.json so user hooks coexist. beforeShellExecution runs the guard adapter and failClosed:true blocks on hook failure. Headless hook behavior is version-sensitive; setup must smoke-test it. The static cli-config fragment covers secret reads, not catastrophic shell commands.",
  "version": 1,
  "hooks": {
    "beforeShellExecution": [
      {
        "command": "\"$HOME\"/.cursor/leos-python \"$HOME\"/.cursor/hooks/guard-adapter.py",
        "failClosed": true
      }
    ]
  }
}
